name: RAG Quality Gate Regression Test

# 1. Trigger the cloud server ONLY when a Pull Request is opened against the main branch
on:
  pull_request:
    branches: [ main ]

jobs:
  run-ragas-evaluations:
    runs-on: ubuntu-latest

    steps:
    # Step A: Download your repository code onto the GitHub Cloud computer
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    # Step B: Set up a clean Python environment matching your local machine
    - name: Initialize Python 3.11 Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip' # Speeds up future runs by caching libraries

    # Step C: Install your project libraries
    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install .

    # GATE 1: Build the temporary Vector Database and Check Uplift
    - name: Run Master Ingestion and Retrieval Uplift Test
      env:
        # Map your repository secret to BOTH variables to ensure complete tool alignment
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
        HUGGINGFACEHUB_API_TOKEN: ${{ secrets.HF_TOKEN }} 
        PYTHONPATH: ${{ github.workspace }}:${{ github.workspace }}/ingests:${{ github.workspace }}/src
      run: |
        # 1. Dynamically build the full 3-collection database on the runner
        python run_all_ingestion.py
        
        # 2. Execute the evaluation script against the freshly populated database
        python tests/eval_retriever.py


    # GATE 2: Pull down live performance speeds directly from Langfuse
    - name: Run Live Production P95 Latency Gate Validation
      env:
        LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
        LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}
        LANGFUSE_HOST: ${{ secrets.LANGFUSE_HOST }}
      run: |
        python tests/eval_latency.py

    # GATE 3: Execute your 3rd exact testing script file
    # This is where GitHub monitors your sys.exit(1) or sys.exit(0) signals!
    - name: Run RAGAS Regression Script
      env:
        # Securely pass your private environment keys from GitHub Secrets to the script run
        GROQ_API_KEY_1: ${{ secrets.GROQ_API_KEY_1 }}
        GROQ_API_KEY_2: ${{ secrets.GROQ_API_KEY_2 }}
        CHATBOT_GROQ_API_KEY: ${{ secrets.CHATBOT_GROQ_API_KEY }}
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        python tests/eval_ragas.py
